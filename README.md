# Viesup

Trích xuất phụ đề / văn bản từ video — đa ngôn ngữ — chạy ngay trong trình duyệt.

> Repo này khởi tạo bằng PR đầu tiên. Toàn bộ ứng dụng nằm trong PR — sau khi merge bạn có thể đổi tên branch này thành `main` (Settings → Branches).
